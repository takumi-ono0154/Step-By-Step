<div class="p-4">
  <h2 class="text-center mb-4 font-bold text-xl">パスワードをお忘れですか？</h2>

  <div class="flex justify-center items-center min-h-screen">
    <div class="p-4">
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "w-full max-w-md" }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="field mb-4 text-center mt-4">
          <%= f.label :email, "メールアドレス" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full" %>
        </div>

        <div class="actions text-center mt-4">
          <%= f.submit "パスワード変更メールを送る", class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="text-center mt-4">
    <%= render "devise/shared/links" %>
  </div>
</div>
